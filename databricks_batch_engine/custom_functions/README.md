# Custom Python functions used by `Data_Movement_Metadata` transformations.
#
# Drop `.py` modules into this folder and reference them from metadata rows via
# `Custom_Function_Name` / `Transformation_Order`. The batch engine auto-imports
# everything here at runtime — see `databricks_batch_engine/runtime_databricks.py`.
